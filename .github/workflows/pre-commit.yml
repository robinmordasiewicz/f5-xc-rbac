# DISABLED: This is a placeholder workflow
#
# To use this workflow:
# 1. Copy the sample from: samples/ci-cd/github-actions/pre-commit.yml.sample
# 2. Review and customize for your environment
# 3. Replace this file with your customized version
#
# See: samples/ci-cd/README.md for detailed setup instructions

# Uncomment and customize the workflow below:

# name: pre-commit
#
# on:
#   pull_request:
#   push:
#     branches:
#       - main
#   workflow_dispatch:
#
# permissions:
#   contents: read
#   pull-requests: read
#
# concurrency:
#   group: ${{ github.workflow }}-${{ github.ref }}
#   cancel-in-progress: true
#
# jobs:
#   lint:
#     name: pre-commit
#     runs-on: ubuntu-latest
#     steps:
#       - name: Setup Required
#         run: |
#           echo "This workflow is disabled by default"
#           echo "See samples/ci-cd/README.md for setup instructions"
#           exit 1
