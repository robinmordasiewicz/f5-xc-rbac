# research.md — CSV-driven sync (Phase 0)

Date: 2025-11-06

Decision: CSV is authoritative for group and membership state within the sync workflow.

Rationale:
- The user explicitly requested that the CSV drive group existence, user creation, and membership assignments. Making the CSV canonical simplifies operational model and matches many provisioning patterns (source-of-truth import).
- Idempotence and dry-run are required to make enforced automation safe for repeated runs.


Alternatives considered:

Open implementation decisions (resolved here):

Resulting actionables (Phase 1 inputs):
